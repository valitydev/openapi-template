description: >
  For authentication of calls, we use [JWT](https://jwt.io).
  The corresponding key is passed in the header.

  ```shell
    Authorization: Bearer {TOKENIZATION|PRIVATE_JWT}
  ```
type: http
scheme: bearer
bearerFormat: JWT

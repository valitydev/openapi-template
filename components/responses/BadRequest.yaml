description: Invalid data provided
content:
  application/json:
    schema:
      description: Error in the provided data
      type: object
      required:
        - code
      properties:
        code:
          type: string
          enum:
            - invalidRequest
        message:
          type: string
